# Release checklist

- Verify the script works with PowerShell 4/5/7.
- Test representative domains for `.com`, `.net`, `.org`, `.ua`, `.com.ua`, `.dp.ua`, `.wine`.
- Ensure the XML output matches the PRTG format.
- Create a git tag such as `v1.0.0` and push it to GitHub.
